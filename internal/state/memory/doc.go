// Package memory holds the stores used when no database is configured, and by
// tests. Each follows the same rules as its Postgres counterpart; everything is
// lost with the process.
package memory
